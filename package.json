{
  "name": "undoc",
  "version": "0.0.1",
  "bin": { "undoc": ".output/server/index.mjs" },
  "files": [".output"],
  "workspaces": ["client"],
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.2.0"
  }
}
